"""f1ml — the F1 race-position prediction package.

This file is what turns the `f1ml/` folder into an importable package.
It can stay almost empty. Its presence is what lets you write, from a
notebook or script:

    from f1ml.model import F1Net
    from f1ml.dataset import F1LapDataset

Optionally, you can re-export the main pieces here so callers can do
`from f1ml import F1Net` instead of the longer path. Leave it for now;
add re-exports later only if the imports start feeling verbose.
"""
